<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/black</item>
        <item name="tooltipForegroundColor">@color/white</item>
    </style>

    <style name="input_post" parent="@style/Theme.AppCompat.Light">
        <item name="hintTextColor">#000000</item>
        <item name="civ_border_color">#000000</item>
        <item name="civ_border_width">20dp</item>
    </style>

    <style name="AppTheme.PopUpTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
</resources>
